# CLAUDE.md -- Pinecone SDK

## Overview

Auto-generated C# SDK for [Pinecone](https://www.pinecone.io/) -- vector database with similarity search, index management, collections, backups, and inference (embeddings + reranking).

This SDK covers two Pinecone API specs merged into one:
- **db_control** (2026-04): Index CRUD, collections, backups, restore jobs
- **inference** (2026-04): Embeddings, reranking, model listing

The **db_data** spec (vector upsert/query/fetch/delete) uses a per-index host URL (`https://{index_host}`) and is not included in this SDK.

## Build & Test

```bash
dotnet build Pinecone.slnx
dotnet test src/tests/IntegrationTests/
```

## Auth

API key auth via `Api-Key` header (native via `--security-scheme`):

```csharp
var client = new PineconeClient(apiKey); // PINECONE_API_KEY env var
```

## Key Files

- `src/libs/Pinecone/openapi.yaml` -- Merged db_control + inference spec (generated by `generate.sh`)
- `src/libs/Pinecone/generate.sh` -- Downloads both specs, merges with Python, converts auth, runs autosdk
- `src/libs/Pinecone/Generated/` -- **Never edit** -- auto-generated code
- `src/libs/Pinecone/Extensions/PineconeClient.Tools.cs` -- MEAI AIFunction tools
- `src/libs/Pinecone/Extensions/InferenceClient.EmbeddingGenerator.cs` -- MEAI IEmbeddingGenerator implementation
- `src/tests/IntegrationTests/Tests.cs` -- Test helper with API key auth
- `src/tests/IntegrationTests/Examples/` -- Example tests (also generate docs)

## Client Architecture

The SDK has sub-clients accessed via properties:
- `client.ManageIndexes.*` -- Index CRUD, collections, backups, restore jobs
- `client.Inference.*` -- Embeddings, reranking, model listing

## MEAI Integration

- **Interface:** `IEmbeddingGenerator<string, Embedding<float>>` on `InferenceClient`
- **Namespace conflict:** Yes -- `Pinecone.Embedding` struct shadows MEAI `Embedding<T>`. Uses `using Meai = Microsoft.Extensions.AI;` alias
- **Provider name:** `"pinecone"`
- **Default model:** `multilingual-e5-large`
- **Features:** Dense embeddings, token usage tracking
- **Access:** `client.Inference` (implements `IEmbeddingGenerator`)

## MEAI Tools

| Tool | Description |
|------|-------------|
| `AsListIndexesTool()` | List all indexes in the project |
| `AsDescribeIndexTool()` | Get detailed info about a specific index |
| `AsEmbedTool()` | Generate text embeddings with hosted models |
| `AsRerankTool()` | Rerank documents by relevance to a query |
| `AsListModelsTool()` | List available embedding and reranking models |
| `AsListCollectionsTool()` | List all collections in the project |

## Endpoints

### Manage Indexes (db_control)
| Operation | Method | Path |
|-----------|--------|------|
| ListIndexes | GET | `/indexes` |
| CreateIndex | POST | `/indexes` |
| DescribeIndex | GET | `/indexes/{index_name}` |
| ConfigureIndex | PATCH | `/indexes/{index_name}` |
| DeleteIndex | DELETE | `/indexes/{index_name}` |
| CreateIndexForModel | POST | `/indexes/create-for-model` |
| ListCollections | GET | `/collections` |
| CreateCollection | POST | `/collections` |
| DescribeCollection | GET | `/collections/{collection_name}` |
| DeleteCollection | DELETE | `/collections/{collection_name}` |
| CreateBackup | POST | `/indexes/{index_name}/backups` |
| ListIndexBackups | GET | `/indexes/{index_name}/backups` |
| ListProjectBackups | GET | `/backups` |
| DescribeBackup | GET | `/backups/{backup_id}` |
| DeleteBackup | DELETE | `/backups/{backup_id}` |
| CreateIndexFromBackup | POST | `/backups/{backup_id}/create-index` |
| ListRestoreJobs | GET | `/restore-jobs` |
| DescribeRestoreJob | GET | `/restore-jobs/{job_id}` |

### Inference
| Operation | Method | Path |
|-----------|--------|------|
| Embed | POST | `/embed` |
| Rerank | POST | `/rerank` |
| ListModels | GET | `/models` |
| GetModel | GET | `/models/{model_name}` |
